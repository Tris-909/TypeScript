const textArr: string[] = ['a', 'b', 'c'];
const date = [new Date(), new Date()];

const arrayInArray: string[][] = [];

// tuple in TypeScript
const tupl:[string, string, boolean, number] = ['haha', 'yo', true, 21];

