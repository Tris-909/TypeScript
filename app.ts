console.log('Run.....!');

enum Role {
    ADMIN,
    READ_ONLY
}

const person = {
    name: 'Tri',
    age: 21,
    hobbies: ['Coding'],
    role: Role.ADMIN
}

if (person.role = Role.ADMIN) {
    console.log('yea....');
}